<div id="materialsHeader">
    <ul class="nav nav-list">
        {% for month, val in articles  %}
        <li>
            <a href="#">{{ month | localizeddate('none', 'none', null, null, 'LLLL YYYY') }} ({{ val }})</a>
        </li>
        {% endfor %}
    </ul>
</div>